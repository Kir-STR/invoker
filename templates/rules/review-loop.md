---
globs: "**/*"
class: discipline
---

# Review loop — детальный workflow

Подробности к `CLAUDE.md § «PR policy / Review loop»` и `§ «Retrospective после мержа»`. Базовое правило (без автономных правок), триаж по 3 опциям — в `CLAUDE.md`; здесь — обработка CI-инфраструктуры, триггер ре-ревью, критерии устаревания (stale), дисциплина rejection-комментов.

## CI-аномалии (не триггерится / квота / 403) — escape-hatch

Когда `review-gate` красный или недоступен по **инфраструктурной** причине, не из-за finding'ов — код не править. Сначала диагностировать инфраструктурный сбой vs реальные findings. Распознаваемые классы:

- троттлинг CI-платформы на серию force-push («no checks reported» 30+ мин);
- исчерпание квоты API ревьюера (`review-bot`);
- ошибка авторизации от checkout-step.

**Выбор восстановления — решение оператора, каждый раз свежий operator-gate (не auto-cascade).** Порядок предпочтения:

1. узкое **недеструктивное** восстановление (retrigger no-op / close+reopen / подождать / rerun), если способно дать валидный прогон на текущем HEAD;
2. **admin override — резерв**, только когда текущий HEAD не может получить достоверный gate, а доказательство предыдущей итерации связано blob-identity с текущим HEAD.

**Митигация:** ограничить частоту force-push; батчить итерации шлифовки локально.

## Inline-цитирование в rejection-комментах

`review-bot` может не видеть gitignored / локальные доки (планы, спеки) — в rejection-комментах цитировать текст секции spec inline, не только путь; так же inline-цитировать секции `.claude/rules/*.md`, на которые опирается обоснование. Формулировать независимо от доступа («цитируем inline, чтобы обоснование не зависело от доступа review-агента»).

**Citation self-verify до публикации** (reject / audit-trail / supplementary comment) — три волатильные точки:

- **reader-accessibility** — не цитировать gitignored / local-only файлы (`ideas_4_rules.md`, `retro-*.md`, `.claude/settings.local.json`, `.env*`): PR-readers не резолвят их ни на каком SHA. Либо inline-quote содержимое, либо reanchor на public-артефакт (commit SHA, merged docs).
- **code file:line anchor** — грепать точную строку в текущем дереве до публикации комментария. Неточность в опубликованном комменте остаётся в истории навсегда.

## Verify upstream guarantee до rejection-комментария

Если rejection опирается на цитирование вышестоящей гарантии (safety / sanitization / валидация / любой контракт на границе модуля) — проверить в коде или parent-plan'е, что вышестоящая логика **уже реализована** в текущем состоянии репо, а не записана только как целевое состояние в операционной выжимке. `.claude/rules/safety.md`, инварианты и т.п. описывают **целевое** поведение; реальное состояние читается из исходного кода и parent-plan'ов активных PR'ов.

## Audit-trail Gate split (publication ↔ execution)

Для escape-hatch admin override — explicit split на Gate 1 (publication) и Gate 2 (execution):

- **Gate 1** — публикация audit-trail комментария в PR. Недеструктивный класс. Требует буквальное «да» оператора на текст комментария.
- **Gate 2** — execution (merge с admin override). Деструктивный класс. Требует **независимое** буквальное «да» — не cascade'ится из Gate 1.

Между Gate 1 и Gate 2 — обязательный свежий пакет перепроверок:
- обновить состояние из источника правды (`git fetch origin` + сверить SHA);
- пересчитать sensitive-paths;
- пересчитать LOC;
- проверить статус CI;
- проверить статус conversation resolution.

**Строгость формулировок в audit-trail:** утверждения строго соответствуют показанному доказательству. Если показано N хешей — не утверждать «all M», где M > N; таблицы-образцы помечать как «representative anchor evidence», не подразумевать «complete proof» через формулировку.
